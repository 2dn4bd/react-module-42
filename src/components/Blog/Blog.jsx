import PropTypes from 'prop-types';
const Blog = ({blogs}) => {
    return (
        <div>
            
        </div>
    );
};
Blog.propTypes = {
    blogs:PropTypes.object.isRequired
}
export default Blog;